# 2026-04-24 — 2nd-brain session

## What shipped

- **PR #34** (merged, main at `2a932f8`): Haiku → mixed-model migration.
  - Five Haiku callsites moved to OpenAI `gpt-5.4-nano`: `processor.ts`, `familyAsk.ts`, `archive/processor.ts` (analyzeArtifact + extractEntities), `archive/ask.ts`. The three forced-tool-use calls were re-expressed as Structured Outputs (`response_format: { type: "json_schema", json_schema: { …, strict: true } }`, with `additionalProperties: false` on every object per strict-mode rules).
  - Morning review (`src/review.ts`) stayed on the Anthropic SDK; model string bumped `claude-haiku-4-5-20251001` → `claude-sonnet-4-6`. Decision: nano for classification / extraction / grounded-RAG answers, Sonnet for the one callsite doing real multi-source synthesis.
  - `max_tokens` → `max_completion_tokens` on all OpenAI calls (required for gpt-5 family).
  - Telemetry rename in `archive/processor.ts`: `response.usage.input_tokens/output_tokens` → `response.usage.prompt_tokens/completion_tokens`. Log lines still show `in=` / `out=`.
  - `@anthropic-ai/sdk` + `ANTHROPIC_API_KEY` retained (still used by `review.ts`). No dep or env-var churn.

## What was tried and abandoned

- **PR #35 → reverted by PR #36.** Misread "deprecate the email worker" as `syncGmail` in `src/google/sync.ts`. Wrong: `syncGmail` is in active use populating `email_ref` for the morning-review Follow-ups section, family `/ask` RAG, and the journal↔email linker. PR #36 restored `sync.ts` exactly. Lesson: "email worker" here meant the Cloudflare build project, not any runtime worker.

## The actual root cause (resolved, no code change needed)

- A Cloudflare Pages/Workers project ("note") was wired to `unsubject/2nd-brain` in the Cloudflare dashboard and ran `npx wrangler deploy` on every push. Since this repo is a Node.js service (not a static site / Worker), wrangler errored with *"Could not detect a directory containing static files"* every single build, flooding PR checks.
- Repo had no `wrangler.toml` / `wrangler.jsonc` / `.github/workflows/` driving this — purely dashboard-side project.
- **Resolved:** user deleted the Cloudflare project in the dashboard.

## Orphan code after the Cloudflare deletion

Now unused, not cleaned up this session:

- `POST /capture` route in `src/bot.ts::startWebhook` (inside `startWebhook`). The `channel: channel || "email"` default is the fingerprint — this endpoint was the receiver for the Cloudflare email Worker.
- `CAPTURE_API_KEY` env var.

Small PR candidate: remove the route, remove the env var from `.env.example`, and drop the `randomUUID` import if nothing else uses it.

## Ops notes for the first Railway rollout of PR #34

- Confirm `OPENAI_API_KEY` in Railway can reach `gpt-5.4-nano` before trusting live traffic. If it can't, every capture errors and gets marked `processing_error`.
- Sonnet 4.6 will produce noticeably longer / more synthesized morning reviews than Haiku did. Obvious knob if it's too verbose: `max_tokens: 3072` cap in `review.ts`.
- Watch `[archive] analyze tokens` / `[archive] entities tokens` log lines — telemetry field names were renamed; if values come through as `undefined`, the OpenAI SDK version may differ from assumed (code uses `response.usage?.prompt_tokens` with optional chaining).

## If this session is resumed

1. Verify first post-#34 Railway deploy is green and one end-to-end journal capture processes cleanly.
2. Observe one morning-review cycle on Sonnet for quality and cost.
3. Decide on the `/capture` + `CAPTURE_API_KEY` cleanup.
